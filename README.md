## RiverApp

This is a Laravel + Vue application

## Install
1. Install Laravel:
composer install
rename env-example to .env

2. Setup Vue and front end structure:
npm run dev

# Serve
To run local server:
php artisan serve

